# sd-trans

对于 SD 格式的 Prompt 接入大模型快速翻译、方便调整。

## 功能

- 按逗号解析 SD prompt/tag 列表。
- 支持 OpenAI 默认接口或自定义兼容接口。
- 支持配置 API Key、模型和目标语言。
- 支持翻译后继续编辑、删除和复制结果。
- 支持浅色、深色和跟随系统主题。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```
