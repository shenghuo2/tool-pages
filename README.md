# 图片对比

一个用于快速对比两张图片差异的小工具。支持上传、拖拽和粘贴图片，在同一画布里用滑块、缩放和平移查看前后差异。

## 功能

- 左右两侧分别导入图片。
- 支持拖拽上传、文件选择和剪贴板粘贴。
- 支持分割线滑块对比两张图片。
- 支持滚轮缩放和平移查看细节。
- 显示图片文件名、尺寸和文件大小。
- 支持中英文界面切换。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

